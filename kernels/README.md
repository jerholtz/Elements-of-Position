# Kernels

Two generators. Never numbered. The only places anything is made.

- `walk/` — shear then bind. Hit = sign change of `y`. Start `(1,0)`.
- `ngon/` — `n` points on a circle of radius `n/2`.

Both files are still named `kernel.py`. Importers load by full path. Rename is deferred to paper consolidation.

Recorded run cited by the current papers:

`walk/runs/2026-09-19_h0.002_n200000/`

After First Order this folder remains as history. The conversation-owned run will be a new dated sibling.
