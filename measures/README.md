# Measures

Derived tables. May import kernels. Do not draw.

- `bridge/` — when walk and n-gon are the same object (`h = tan(2π/n)`). CORDIC ceiling at `n=3,4`.
- `already-there/` — Bernoulli ladder, gap word, Stern–Brocot. Parked. Still contains a copied `walk()` until the fresh-run wiring.

Claims *about* a kernel (Archimedes sandwich, `π²/(2n²)` squeeze) belong here if they are ever written. They do not go back into `kernels/ngon/kernel.py`.
