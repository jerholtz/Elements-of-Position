# Geometric kernel history

Provenance only. Live papers do not cite this folder.

Lineage, oldest to newest in this folder:

1. `Original/` — `Geometric Kernel.py` and the expand / pairs / wedge / horizon-snap / N3600 / vary-h runs.
2. `Elements of Position . Geometric Kernel.py`, `EOP_Geometric_Kernel_minimal.py`, `The Geometric Kernel.tex`.
3. `eop_kernel_out/` — early crossings / pairs / summary from that generator.
4. `EOP - Kernel.py` — immediate predecessor of `kernels/walk/kernel.py`.
5. `main-kernel-run-snapshot/` — the recorded `h=0.002`, `200000`-step tables. Byte-identical to `kernels/walk/runs/2026-09-19_h0.002_n200000/`. Kept so the prefinal `runs/Main Kernel/` path remains reconstructable. The live citation target is the walk-kernel folder, not this copy.

`README.prefinal.md` is the one-page pipeline note that sat at the root of the prefinal dump.
