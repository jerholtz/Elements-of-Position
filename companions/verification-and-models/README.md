# Companion: Verification and Models

Family 2. Its own visual language (serif titles, plain grid, no title block).

Formulas here are supposed to be reimplemented from the kernels, not read from their CSVs — an independent second route to the same numbers, on purpose.

## What is in this snapshot

- `companion.tex` — *Elements of Position. Companion: Verification and Models* (13 September 2026).

## What is not in either supplied zip

The original structure note lists `local_circle_models.py`, `verification.py`, `measurements.csv`, `docking_events.csv`, HTML/PNG models. Those files were not in the September package or the prefinal dump. This folder is incomplete until they arrive. Do not invent them.
